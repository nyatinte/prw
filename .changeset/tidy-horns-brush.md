---
"@nyatinte/prw": patch
---

Support running `prw` from workspace subdirectories by resolving the nearest pnpm workspace root before listing packages or running scripts.

Improve the error shown when `prw` is started completely outside a pnpm workspace.
