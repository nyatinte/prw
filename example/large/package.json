{
  "name": "large-workspace",
  "version": "1.0.0",
  "scripts": {
    "prw": "node ../../dist/bin.mjs",
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
